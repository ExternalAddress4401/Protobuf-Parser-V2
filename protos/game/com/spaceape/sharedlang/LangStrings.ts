import { CMSField } from "../../../../../interfaces/CMSField";

export const LangStrings: Map<number, CMSField> = new Map([
  [1, { name: "key", type: "string" }],
  [2, { name: "value", type: "string" }],
]);
